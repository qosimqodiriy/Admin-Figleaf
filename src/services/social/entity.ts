export interface Social {
   id: number,
   priority: number,
   url: string,
   icon: string
}

export interface Form {
   id: number | null,
   priority: number | null,
   url: string | null,
   icon: string | null
}

